# --- GUI Framework ---
PySide6>=6.11.0

# --- Core AI & Vision ---
# ONNX Runtime (CPU). For GPU builds substitute onnxruntime-gpu (system
# CUDA 12 + cuDNN 9 required) or onnxruntime-directml (Windows, any DX12 GPU).
onnxruntime>=1.23.0
# Downloads SAM 2 ONNX weights from Hugging Face on first use.
huggingface_hub

# --- Image Processing & Math ---
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0

# --- Packaging (Optional) ---
# Only needed if you want to build a standalone executable.
pyinstaller
